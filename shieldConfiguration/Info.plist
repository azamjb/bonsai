<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
    "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>NSExtension</key>
    <dict>
        <!-- Add this 'NSExtensionAttributes' block with 'ManagedSettingsUIExtension' set to true. -->
        <key>NSExtensionAttributes</key>
        <dict>
            <key>ManagedSettingsUIExtension</key>
            <true/>
        </dict>

        <!-- Keep the existing extension point identifier for a Shield Configuration Extension. -->
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.ManagedSettingsUI.shield-configuration-service</string>

        <!-- Ensure the principal class matches your ShieldConfigurationExtension subclass. -->
        <key>NSExtensionPrincipalClass</key>
        <string>$(PRODUCT_MODULE_NAME).ShieldConfigurationExtension</string>
    </dict>
</dict>
</plist>
